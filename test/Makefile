KOMPILE=llvm-kompile-testing
DEFN = $(wildcard defn/*.kore)
INPUT = $(wildcard input/*.in.kore)
OUTPUT= $(wildcard output/*.out.kore)

all:
	@echo "1"
	@echo $(DEFN)
	@echo "2"
	@echo $(INPUT)
	@echo "3"
	@echo $(OUTPUT)
